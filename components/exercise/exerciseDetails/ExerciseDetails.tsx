

type ExerciseDetailsProps = {
    exerciseId: number,
    exerciseConfigId: number
  }
  
export const ExerciseDetails: React.FC<ExerciseDetailsProps> = ({ exerciseId, exerciseConfigId }) => {
    // show name
    // show all of the with deletion button
    return null;
}